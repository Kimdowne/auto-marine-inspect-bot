# AI 기반 선박 안전 자동진단 로봇 프로젝트 컨텍스트 정리

## 1. 담당자 및 담당 분야

- 담당자: 김수미(Windows 사용자명 및 W&B 계정명 기준, 최종 확인 필요)
- 담당 분야: Unity 기반 이동로봇 자율주행, AprilTag 모사 경로 항법, RGB·ToF 기반 사람 인지, PPO 동적 장애물 회피
- 수행 기간: 대화에서 정확한 시작일 확인 불가. 2026년 8~9월 스크린샷과 실행 기록이 존재함.
- 다른 모듈과의 연결 관계: 이동 제어 결과를 설비 점검 위치 `inspect_point_A1`, `inspect_point_A2`와 연결한다. 설비 상태 진단·진동 분석 모듈 자체의 개발 내용은 이 대화 범위에 포함되지 않는다.

## 2. 담당 모듈의 목적

- 해결하려는 문제: 선박 기관실 형태의 좁은 통로에서 로봇이 노란색 차선을 따라 지정 경로를 순환하고, 삼거리에서 경로를 선택하며, 전방 보행자를 충돌 없이 회피한 뒤 차선 주행으로 복귀하도록 한다.
- 입력: 전방 RGB 카메라 영상, 좌·우 가상 ToF 거리·접근속도·TTC, HSV 차선 검출 결과, 시뮬레이션 AprilTag 관측, 로봇 선속도·각속도.
- 처리 과정: HSV 차선 검출 및 PD 제어 → 태그/경로 그래프에 따른 교차로 상태 전이 → YOLOX 사람 위치와 두 ToF 거리 융합 → PPO 연속 회피 명령 → 위험 해소 후 규칙 기반 차선 복귀.
- 출력: 4개 WheelCollider의 좌·우 구동 명령, 임무 상태, 사람 박스, ToF/TTC, PPO 명령 및 에피소드 결과 로그.
- 전체 시스템에서의 역할: 설비 점검 지점까지 안전하게 이동하는 주행·안전 계층이다.

## 3. 실제 구현 내용

| 구현 항목 | 사용 기술·장비 | 구체적인 동작 | 완료 상태 | 근거 |
|---|---|---|---|---|
| HSV 차선 추종 | Unity C#, 전방 카메라, HSV 선 검출, PD 제어 | 횡오차·방향오차·미분항으로 회전 명령을 계산하고 confidence에 따라 속도를 조절 | 구현 완료, 외곽 순환에서 정성 확인 | `HsvLaneDetector`, `LaneFollowerController`; 사용자 보고 “한 바퀴 도는 거 성공” |
| 경로 그래프 및 외곽 순환 | 6개 노드, Dijkstra 최단경로, 시뮬레이션 AprilTag | `UnderMid → UpperMid → UpperLeft → UnderLeft → UnderMid → UnderRight → UpperRight → UpperMid` 경로 지원 | 구현 및 1회 완주 확인 | `PlantRouteGraph`, `MissionRoutePlanner`, 사용자 실행 결과 |
| 삼거리 통과 상태기계 | 차선 양측 경계, 태그, 수동 직진/회전 명령 | 진입 경계 한쪽이 30프레임 사라진 뒤 회전·정렬하며, 직진 분기에서는 차선 재획득 또는 태그 fallback 사용 | 구현 완료, 외곽 순환으로 정성 검증 | `NavigationCoordinator`; `requiredSideLossFrames=30` |
| 설비 A 점검 경로 | `inspect_point_A1`, `inspect_point_A2` | 점검점 반경 진입 시 정지하고 점검 상태로 전환한 뒤 경로 복귀 | 코드 및 씬 연결 구현, 종단 간 임무 검증은 확인 필요 | `NavigationCoordinator`, `PlantNavigationSetupEditor` |
| RGB 사람 검출 | 공식 Megvii YOLOX-S 640 ONNX, Unity Inference Engine/Sentis, GPUCompute | COCO person score, NMS, 최적 박스를 사용해 confidence·수평 중심·박스 폭/높이를 출력 | 구현 완료, 사람이 움직일 때 박스 추종 정성 확인 | `OfficialYoloxPersonDetector`, `RgbPersonDetector`, 사용자 스크린샷 |
| YOLOX fallback | BlazePose | YOLOX 모델 로드 실패 시 BlazePose 검출기로 전환 | 구현 완료, 주 검출기는 YOLOX로 확정 | `RgbPersonDetector.Awake()` |
| 전방 이중 ToF 모사 | 좌·우 `VirtualToFSensor` | 각 센서가 20° FOV에서 복수 ray의 최소 거리를 반환하고 거리·closing speed·TTC 계산 | 구현 완료, 패널에서 동작 확인 | `VirtualToFSensor`, `DualToFSensorRig` |
| PPO 학습 환경 | Unity ML-Agents, Python PPO, W&B | 보행자 접근 시나리오 생성, 관측·보상·종료 결과 기록 | 기존 이산 정책은 25만 스텝 실험, 성능 미달 | ML-Agents 로그 및 W&B 차트 |
| 연속 PPO 회피 | 연속 액션 2개 | PPO가 회피 중 전진/후진과 yaw를 직접 제어하며 차선/ToF 감속이 PPO 명령을 덮어쓰지 않게 제어권 분리 | 코드 구현·C# 컴파일 완료, 재학습 및 실제 주행 검증 전 | `HumanAvoidanceAgent`, `LaneFollowerController` |
| 회피 후 차선 복귀 | 규칙 기반 저속 정렬 | 차선이 보이면 오차 기반 저속 정렬, 안 보이면 회피 진입 전 heading으로 제자리 회전, 8초 실패 시 정지 | 코드 구현·컴파일 완료, 런타임 검증 전 | `UpdateLaneRecovery()` |
| W&B 직접 로깅 | 사용자 정의 StatsWriter | 에피소드 보상·성공·충돌·실패 원인·센서·PPO 명령을 W&B로 전송 | 구현 완료, 최신 연속 정책의 장기 로그 검증 전 | `tools/train_human_avoidance_wandb.py` |
| 체크포인트 장애 격리 | PyTorch `.pt`, best-effort ONNX | `.pt` 저장 후 ONNX 변환이 실패해도 학습과 W&B를 계속하도록 예외 처리 | 격리 단위시험 완료, 25만 스텝 종단 시험 전 | 강제 `ModuleNotFoundError` 시험 통과 |

## 4. 시스템 구성

- 하드웨어: 실제 하드웨어 구성은 확인 필요. Unity 씬에서는 JetBot 형태 4륜 skid-steer 로봇, 전방 RGB 카메라, 좌·우 가상 ToF 센서를 사용한다.
- 소프트웨어: Unity `6000.5.7f1`, Unity ML-Agents 패키지 `4.1.0`, Python 측 `ml-agents 1.1.0`, PyTorch, W&B.
- 센서: RGB 카메라 640×640 추론 입력, 가상 ToF 2개. 물리 센서로는 VL53L0X v2(2 cm~2 m)를 염두에 두었으나 실제 센서 연동·교정은 확인 필요.
- 제어보드 및 연산장치: 대화에서 실제 보드/연산장치 확인 불가. RGB 추론 backend는 Unity `GPUCompute`로 설정됨.
- 통신 방식: Unity–Python ML-Agents communicator(실행 로그에서 port 5004), W&B 직접 클라우드 로깅. 실제 로봇 센서/모터 통신은 확인 필요.
- 사용 언어 및 라이브러리: C#, Python, Unity Inference Engine/Sentis, Unity ML-Agents, PyTorch, wandb.
- 데이터 저장 및 전달 방식: `results/<run-id>/`에 `.pt` 체크포인트와 이벤트 파일, `wandb/run-*`에 로컬 run 기록, W&B 서버에 학습 지표.
- 주요 설정값 및 파라미터:
  - 차선: 최소 confidence 0.30, 최대 관측 age 0.20 s, cruise command 0.55, corner minimum 0.20, 최대 turn 0.75, motor torque 5, slew rate 3.
  - 태그: 검출 거리 4.00 m, 최소 confidence 0.45, 연속 3프레임 확인.
  - 삼거리: 한쪽 경계 소실 30프레임, 최대 접근 거리 1.50 m, 직진 차선 재획득 최대 3.0 m.
  - RGB: YOLOX-S 640, confidence 0.20, inference interval 0.15 s, 관측 age 0.50 s, NMS IoU 0.45.
  - ToF: 최대 2.0 m, 20° FOV, 씬 setup 시 ray 7개, noise 표준편차 0.01 m, rig sample rate 20 Hz, smoothing 0.35.
  - PPO: decision interval 0.2 simulation s, 관측 18개×4 stack, 연속 액션 2개, time scale 10.
  - PPO trainer: batch 128, buffer 1024, learning rate 0.0003, beta 0.005, epsilon 0.2, lambda 0.95, epoch 3, hidden units 256×2, gamma 0.99, horizon 128, summary 2,000 step, checkpoint 250,000 step, max 3,000,000 step, 체크포인트 5개 유지.

## 5. 알고리즘 또는 제어 과정

1. RGB 영상에서 YOLOX-S가 사람 후보를 검출한다. confidence는 `objectness × COCO person score`이며, NMS 후 가장 confidence가 높은 유효 박스를 선택한다. 최근 3회의 추론 중 2회 이상 검출되어야 `HasPerson`이 된다.
2. 좌·우 ToF는 각각 FOV 내부 ray 중 최소 거리를 측정한다. smoothing 후 접근속도는 `max(0, (이전 거리-현재 거리)/Δt)`로 계산한다.
3. TTC는 `max(0, 거리-0.20 m)/접근속도`이다. 접근속도가 없으면 무한대로 처리한다.
4. PPO 관측은 좌·우 거리(2), 접근속도(2), TTC(2), 사람 유무·수평 위치·confidence·박스 폭·높이(5), 차선 횡오차·방향오차·confidence(3), 기존 이동·회전 명령(2), yaw rate(1), 전진속도(1)의 총 18개이다. 이를 4프레임 stack한다.
5. 최신 정책의 액션은 `move ∈ [-1,1]`, `turn ∈ [-1,1]` 두 연속값이다. 회피가 활성화되면 PPO가 WheelCollider 구동 계층을 직접 점유하며 기존 차선 confidence, 차선 offset 및 ToF 감속은 이 명령을 변경하지 않는다. 모터 torque 한계와 slew limiter는 유지한다.
6. 회피 해제 조건은 사람 미검출, 전방 최소거리 1.60 m 이상 상태를 0.60 s 유지하는 것이다. 배치 학습에서는 에피소드 종료 조건이 우선하며, 배포 모드에서 해제 뒤 차선 복귀 상태로 전환한다.
7. 복귀 시 차선이 보이면 `turn = clamp(0.85×횡오차 + 0.55×방향오차, -0.5, 0.5)`, move 0.2로 정렬한다. 차선이 없으면 이동 없이 진입 전 heading으로 회전한다. 8 s 내 복귀하지 못하면 정지한다.
8. 일반 주행에서는 PD 식 `turn = clamp(0.85×횡오차 + 0.55×방향오차 + 0.08×오차미분, ±0.75)`을 사용한다.
9. 태그 경로에서는 노드 그래프와 이전–현재–다음 노드 방향으로 직진/좌회전/우회전을 결정한다. 진입 경계 소실을 확인한 뒤 회전하고, 두 경계를 재검출해 차선 주행으로 복귀한다. 직진 분기에서 차선이 사라지면 다음 태그가 보일 때까지 저속 직진하는 fallback이 있다.

현재 보상은 매 physics step `-0.0005`, 연속 액션 변화량에 `-0.002×(|Δmove|+|Δturn|)`, 위험거리 비례 penalty 최대 `-0.004`를 사용한다. 사람이 사라지고 거리가 1.8 m 이상일 때만 전진 progress reward가 적용된다. 성공 `+2`, yield 성공 `+1`, natural safe pass `+0.05`, passive failure `-1`, emergency/timeout `-2`, 충돌 `-5`이다. 기존 offset penalty 코드는 남아 있으나 최신 연속 제어에서는 target offset이 0이므로 사실상 비활성이다.

## 6. 데이터 구성

- 데이터 출처: Unity 시뮬레이션에서 에피소드별 온라인 생성.
- 측정 대상: 보행자와 로봇의 상대 접근 상황, RGB 사람 검출, 좌·우 ToF, 차선 오차, 로봇 동작과 충돌 결과.
- 정상 데이터 수: 고정 데이터셋이 아니므로 해당 없음/확인 필요.
- 비정상 데이터 수: 고정 데이터셋이 아니므로 해당 없음/확인 필요.
- 전체 데이터 수: 최신 목표는 3,000,000 trainer step. 과거 실험은 250,000 step까지 기록됨.
- 샘플링 주파수: ToF 20 Hz, RGB 약 6.67 Hz(0.15 s 간격), PPO 5 Hz의 simulation time 기준(0.2 s 간격). Unity physics timestep은 확인 필요.
- 1회 측정 시간: 에피소드 최대 simulation time 28 s, 보행자 경로 완료 후 0.50 s hold 시 조기 종료.
- 학습·검증·시험 분할: 별도 고정 분할 없음. 온라인 시뮬레이션 학습만 존재하며 독립 평가 seed/시험 세트는 아직 없음.
- 전처리 방법: 거리 2 m 기준 정규화, closing speed 2 m/s 기준 정규화, TTC 5 s 범위 정규화, 사람 수평 위치 [-1,1], 속도·각속도 clamp, YOLOX 640×640 입력.
- 정상과 비정상의 생성 또는 구분 방법: 보행자 궤적의 정지 로봇/명목 직진 경로에 대한 예상 clearance로 active avoidance, yield, natural pass를 구분한다.
- 확인이 필요한 데이터 정보: random seed, 총 에피소드 수의 정확값, 각 시나리오별 실제 표본 수, 독립 검증/시험 구성, 실환경 데이터 존재 여부.

시나리오 기본값은 보행자 시작 거리 4~6 m, 접근각 ±12° 이내, 보행 속도 0.30~0.60 m/s, 로봇 인근 목표 통과 후 추가 이동 1.0~1.5 m이다. Foundation 비율은 active/yield/natural = 80/20/0%, Mixed는 60/25/15%이며 최근 100개 active 에피소드 성공률 0.70 이상에서 자동 전환하도록 구현되어 있다. 생성 조건 만족 시도 횟수는 8회이다.

## 7. 실험 방법

- 실험 목적: (1) 외곽 경로 완주 가능성 확인, (2) RGB 사람 박스 및 ToF 거리/TTC 동작 확인, (3) PPO가 정지 편향 없이 사람을 회피하는지 평가.
- 실험 환경: Unity Editor 및 Windows standalone build, time scale 10, Python ML-Agents trainer, W&B 대시보드.
- 비교 방법 또는 모델: 규칙 기반 차선 추종과 PPO 회피를 계층적으로 결합. 이산 LEFT/RIGHT 정책을 먼저 실험했고 성능 문제 후 연속 move/turn 정책으로 변경했다. 정식 baseline 비교 실험은 아직 없음.
- 평가 지표: cumulative/final reward, success, collision, timeout, passive failure, minimum distance, maximum forward speed, active/yield scenario별 성공률, PPO move/turn, 센서와 차선 confidence.
- 실험 절차: Unity 씬 또는 빌드를 실행하고 trainer에 연결 → 무작위 접근 보행자 생성 → 관측 4-stack을 PPO에 전달 → 0.2 s마다 액션 갱신 → 충돌/보행 완료/timeout으로 종료 → ML-Agents 및 W&B 기록.
- 반복 횟수: 외곽 순환은 성공 1회가 대화에서 명시됨. 과거 PPO는 250,000 step까지 수행했으며 W&B 그림상 약 900개 에피소드로 보이나 정확한 수는 확인 필요.
- 재현에 필요한 설정: `config/human_avoidance_ppo.yaml`, Behavior Name `HumanAvoidance`, continuous action 2, vector observation 18, stack 4, Behavior Type `Default`, SafetySupervisor `MONITOR ONLY`(`enforceControl=0`). 최신 Unity player를 다시 build하고 과거 이산 checkpoint를 resume하지 않아야 한다.

## 8. 실험 결과

| 대상 또는 모델 | 평가 지표 | 결과 | 시험 샘플 수 | 비고 |
|---|---:|---:|---:|---|
| 외곽 순환 항법 | 완주 | 1바퀴 성공 | 1회 확인 | 정성 시험이며 반복 성공률 없음 |
| 과거 PPO run | trainer step | 250,000 | 약 900 episodes(차트 판독, 확인 필요) | 최신 연속 정책 이전 실험 |
| 과거 PPO run | Mean Reward | -2.384 | 해당 250,000-step summary window | ML-Agents 콘솔 원문 |
| 과거 PPO run | Reward Std | 0.129 | 해당 summary window | ML-Agents 콘솔 원문 |
| 과거 PPO run | overall success trend | 마지막 약 0.27 | rolling 100, 정확값 확인 필요 | 제공된 W&B 차트 육안 판독 |
| 과거 PPO run | yield success trend | 마지막 약 0.86 | rolling 100, 정확값 확인 필요 | natural/pass 비율 및 정지 편향 영향 주의 |
| ONNX 실패 격리 | 예외 전파 여부 | 강제 `ModuleNotFoundError`가 trainer 밖으로 전파되지 않음 | 단위시험 1회 | 장기 체크포인트 시험 필요 |
| 최신 연속 PPO | C# 컴파일 | 오류 없이 통과 | 1회 | 학습 성능 결과는 아직 없음 |

- 가장 성능이 좋은 방법: 외곽 순환은 현재 규칙 기반 차선·태그 상태기계가 실제 완주된 유일한 방법이다. 동적 회피 모델은 성능 우열을 확정할 실험이 없다.
- 주요 관찰 결과: YOLOX 박스는 최종 수정 후 사람 움직임을 따라갔다. 기존 PPO는 사람이 자연히 지나가도 성공으로 처리되어 정지 또는 소극적 정책을 학습할 가능성이 확인되었다.
- 실패 사례: 삼거리에서 너무 일찍 회전해 기존 선을 새 차선으로 오인, 직진 분기에서 한쪽 선 소실 후 정지, RGB stale frame/full-frame box, ToF/TTC 표시 오류, Unity–Python timeout, ONNX checkpoint 변환 시 `onnxscript` 부재, Windows 애플리케이션 제어 정책에 의한 DLL 차단.
- 결과 해석 시 주의사항: 25만 step 결과는 이산 방향 액션, 과거 보상 및 시나리오 구조의 결과다. 최신 연속 제어 모델의 성능으로 인용하면 안 된다.
- 데이터 누수 또는 과적합 검토 여부: 독립 seed 평가, train/test 분리, 과적합 검토를 수행하지 않았다. 따라서 일반화 성능을 주장할 수 없다.

## 9. 계획 단계이거나 미완성인 내용

| 항목 | 현재 상태 | 남은 작업 | 논문에 쓸 때 주의할 점 |
|---|---|---|---|
| 연속 PPO 장기 학습 | 코드·씬 설정 완료 | 새 build로 3M step 학습, 학습곡선 확인 | 과거 25만 step 수치를 최신 모델 성능으로 쓰지 말 것 |
| 체크포인트 지속성 | 예외 격리 단위시험 완료 | 250k와 500k 통과 및 W&B 연속 기록 확인 | 아직 장기 검증 결과가 아님 |
| ONNX 배포 모델 | `.pt` 저장 가능 | 차단되지 않은 `onnxscript` 환경에서 최종 ONNX 변환 | 변환 실패 상태에서는 Unity 추론 모델 배포 불가 |
| 회피 후 차선 복귀 | 알고리즘 구현·컴파일 완료 | 사람 통과 후 재획득률, 시간, 경로이탈률 측정 | 검증 전이므로 “성공”으로 표현 금지 |
| 측면·후방 안전 | 현재 의도적으로 제외 | 센서 추가 또는 360° 추적, 정책 확장 | 현재는 전방 접근 시나리오 전용 |
| 실제 VL53L0X 연동 | 시뮬레이션 모델만 구현 | I2C/보드 연동, 오차·반사율·FOV 교정 | 가상 ray 결과를 실제 센서 성능으로 주장 금지 |
| 실제 AprilTag 인식 | 가시성·크기 기반 모사 | 실제 영상 decoder와 pose estimation 연결 | 현재 구현은 실제 AprilTag decoding이 아님 |
| 설비 점검 종단 임무 | 위치/상태 코드 존재 | A1/A2 접근·점검·복귀 반복 시험 | 이동 경로 구현과 진단 정확도는 별도 평가 필요 |
| 독립 성능 평가 | 미실시 | 고정 seed 시험 세트, baseline, 반복실험과 신뢰구간 | 현재 결과는 탐색적 실험 수준 |

## 10. 논문에 사용할 수 있는 핵심 기여

1. HSV 차선 추종과 시뮬레이션 AprilTag 경로 그래프를 결합하고, 삼거리 진입 경계 소실·회전·차선 재획득을 상태기계로 처리하여 외곽 순환 1회를 완주하였다.
2. YOLOX-S 사람 수평 위치와 좌·우 2 m ToF의 거리·접근속도·TTC를 결합한 18차원 시계열 관측 및 W&B 에피소드 진단 파이프라인을 구현하였다.
3. 회피 중 PPO의 연속 이동·회전 제어권과 일반 차선 추종 제어권을 분리하고, 위험 해소 뒤 규칙 기반 차선 복귀로 전환하는 계층 구조를 구현하였다. 단, 이 구조의 학습 성능은 검증 전이다.

## 11. 논문용 문장 후보

- 제안 방법 설명: 본 연구는 선박 기관실의 제한된 통로에서 차선 기반 자율주행과 학습 기반 동적 장애물 회피를 결합하는 계층형 제어 구조를 제안한다. 정상 구간은 HSV 기반 PD 차선 제어가 담당하고, 전방 보행자 위험 구간에서는 PPO가 연속 이동 및 회전 명령을 생성하며, 위험 해소 후 규칙 기반 정렬기가 차선을 재획득한다.
- 구현 내용 설명: Unity 환경에 6개 경로 노드와 시뮬레이션 AprilTag 관측원을 구성하고, 교차로 진입·회전·직진·재정렬을 상태기계로 구현하였다. 사람 인지는 YOLOX-S 640과 좌·우 가상 ToF를 사용하며 거리, 접근속도, TTC 및 영상 내 사람 위치를 강화학습 관측으로 제공한다.
- 실험 방법 설명: 보행자는 로봇 전방 4~6 m, ±12° 범위에서 0.30~0.60 m/s로 접근하도록 온라인 생성하였다. PPO는 18개 관측을 4프레임 적층하여 0.2 simulation s마다 연속 이동·회전 명령을 출력하고, 충돌·보행 완료·28 s timeout을 기준으로 에피소드를 종료한다.
- 실험 결과 설명: 규칙 기반 경로 항법은 외곽 순환 1회를 완주하였다. 반면 이전 이산 PPO 실험은 250,000 step에서 평균 보상 -2.384를 기록하여 충분한 수렴을 확인하지 못했으며, 최신 연속 정책의 정량 결과는 추가 학습 후 제시해야 한다.
- 한계 및 향후 연구: 현재 사람 회피 시나리오는 전방 접근에 한정되고 실제 VL53L0X 및 AprilTag 영상 인식이 연결되지 않았다. 향후 독립 seed 기반 시험 세트에서 충돌률, 최소 clearance, 임무 완료율을 측정하고 측면·후방 인지와 실환경 센서 오차를 포함해야 한다.

## 12. 그림과 표로 제공할 수 있는 자료

- 시스템 구성도: `RGB camera → YOLOX`, `dual ToF → distance/TTC`, `lane detector → lane state`, `PPO → continuous drive`, `recovery → lane follower` 흐름. 신규 작성 필요.
- 하드웨어 사진: 실제 하드웨어 사진은 대화에 없음.
- 데이터 처리 흐름도: 위 알고리즘 1~9단계를 기반으로 작성 가능.
- 진동 신호 그래프: 이 대화 범위에 없음.
- FFT 결과: 이 대화 범위에 없음.
- 혼동행렬: 사람 검출 및 회피 정책 모두 없음.
- 성능 비교표: 최신 연속 PPO 평가 후 작성 필요.
- 기타 자료:
  - `codex-clipboard-0956b622-9983-4325-bad7-1e624a72c5e9.png`: BlazePose 박스 추종 단계.
  - `codex-clipboard-4fb28893-e26e-4da1-b5aa-4f483c7de8d9.png`: YOLOX, RL I/O, Safety monitor, dual ToF 통합 화면.
  - `codex-clipboard-0166d8cb-921f-4462-ac7a-ee84da0a4dba.png`: 과거 episode/final_reward.
  - `codex-clipboard-cab8d539-3eb5-4727-85e7-248bab44a8ed.png`: 과거 episode/success.
  - `codex-clipboard-06ca3f2b-08a8-4c09-9888-6000220c0880.png`, `codex-clipboard-fbadd3e5-11ee-4c23-87f8-bbcbb1d23201.png`, `codex-clipboard-90ca0c97-58cd-4f2d-b238-c432bf75eb4a.png`: 250k-step W&B 지표. 파일은 임시 폴더에 있어 제출 전 별도 보존 필요.

## 13. 참고문헌 후보

- YOLOX: 대화에서 공식 Megvii YOLOX-S 모델을 사용했으나 정확한 논문 서지정보는 별도 확인 필요.
- BlazePose: fallback 구현이 언급되었으나 정확한 논문 서지정보 확인 필요.
- PPO: Unity ML-Agents trainer에서 사용했으나 원 논문 서지정보 확인 필요.
- VL53L0X v2 ToF 센서: 2 cm~200 cm 범위가 대화에서 언급되었으나 제조사 데이터시트와 동작 조건 확인 필요.
- Unity ML-Agents 및 Unity Inference Engine/Sentis 문서: 버전별 공식 문서 서지정보 확인 필요.

## 14. 최종 확인이 필요한 사항

1. 최신 연속 PPO를 새 run-id로 3,000,000 step까지 학습했을 때의 성공률, 충돌률, 최소 clearance 및 reward 곡선.
2. 고정된 독립 평가 seed/시나리오 세트를 구성하고 학습과 시험을 분리했는지 여부.
3. 체크포인트 250,000 step 이후 500,000 step 이상에서도 학습과 W&B 로그가 연속되는지 여부.
4. 성공 판정이 단순 보행자 경로 완료가 아니라 실제 회피 행동과 안전 clearance를 충분히 반영하는지 재검증.
5. 외곽 순환 반복 횟수와 성공률, 구간별 실패 횟수 및 평균 완료 시간.
6. `inspect_point_A1`, `inspect_point_A2` 점검 임무의 종단 간 성공 여부와 점검 데이터 연결 방식.
7. 실제 JetBot/구동보드/RGB 카메라/VL53L0X의 정확한 모델, 장착 위치와 통신 방식.
8. 실제 AprilTag decoder 및 pose estimation 사용 여부. 현재 코드는 가시성 기반 시뮬레이션임.
9. 최신 코드의 사용하지 않는 과거 offset/이산 decision 지표를 정리하고 논문 지표 정의와 일치시키기.
10. 담당자 이름과 정확한 수행 기간.

## 15. 핵심 요약

Unity 선박 기관실 환경에서 HSV 차선 추종, 6노드 AprilTag 모사 경로 항법, 삼거리 상태기계, YOLOX-S 사람 검출, 좌·우 2 m ToF/TTC 및 W&B 학습 진단을 구현했다. 외곽 순환 1회 완주를 확인했으나, 과거 이산 PPO는 25만 step 평균 보상 -2.384로 성능이 부족했다. 현재 PPO가 연속 이동·회전을 직접 제어하고 위험 해소 후 규칙 기반으로 차선을 복귀하는 구조로 변경했으며 컴파일은 통과했지만 장기 학습·독립 시험·실센서 검증은 아직 필요하다.
